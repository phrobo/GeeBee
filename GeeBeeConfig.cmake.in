set(GEEBEE_VERSION_MAJOR @GEEBEE_VERSION_MAJOR@)
set(GEEBEE_VERSION_MINOR @GEEBEE_VERSION_MINOR@)
set(GEEBEE_VERSION_PATCH @GEEBEE_VERSION_PATCH@)
set(GEEBEE_VERSION_PLATFORM @GEEBEE_VERSION_PLATFORM@)
set(GEEBEE_DISCOVERY_PLUGIN_PATH @GEEBEE_DISCOVERY_PLUGIN_PATH@)

find_package(PkgConfig)

pkg_check_modules(GEEBEE QUIET geebee-${GEEBEE_VERSION_PLATFORM})

get_filename_component(GEEBEE_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
